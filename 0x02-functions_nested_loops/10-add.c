#include "main.h"
/**
  * add - it adds the two given inputs
  * @x: The first inger
  * @y: The second integer
  * Return: returns value
  */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
